spring: 
 application: 
  name: gateway-zuul # 应用名

management: 
  endpoints:
    web:
      exposure: 
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS

#配置info信息      
info: 
  app.name: zuul-proxy
  company.name: www.forest.com
  build.artifactId: $project.artifactId$
