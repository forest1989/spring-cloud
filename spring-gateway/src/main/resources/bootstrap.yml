spring: 
 application: 
  name: spring-gateway # 应用名

management: 
  endpoints:
    web:
      exposure: 
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS

#配置info信息      
info: 
  app.name: spring-gateway
  company.name: www.guoshilin.cn
  build.artifactId: $project.artifactId$
  build.version: $project.version$
